DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/infraautopilot OPENAI_API_KEY=sk-... LLM_MODEL=gpt-4.1 LLM_MAX_TOKENS=4000 EXTRACTION_CONFIDENCE_THRESHOLD=0.75 EXTRACTION_AUTO_APPROVE_THRESHOLD=0.90 AZURE_TENANT_ID=... AZURE_CLIENT_ID=... AZURE_CLIENT_SECRET=... AZURE_SUBSCRIPTION_ID=... SECRET_KEY=change-me LOG_LEVEL=INFO ``` --- ## FOLDER STRUCTURE ``` infra-autopilot/ ├ ── backend/ │ ├ ── app/ │ │ ├ ── core/ │ │ │ └── types.py ← SINGLE SOURCE OF TRUTH │ │ ├ ── config.py │ │ ├ ── database.py │ │ ├ ── main.py │ │ ├ ── models/ │ │ │ ├ ── __init__.py │ │ │ ├ ── job.py │ │ │ ├ ── document.py │ │ │ ├ ── prerequisite.py │ │ │ ├ ── scan_result.py │ │ │ ├ ── report.py │ │ │ └── review.py │ │ ├ ── api/ │ │ │ ├ ── deps.py │ │ │ ├ ── documents.py │ │ │ ├ ── jobs.py │ │ │ ├ ── scans.py │ │ │ ├ ── reports.py │ │ │ └── review.py │ │ ├ ── extraction/ ← ENGINE 1 │ │ │ ├ ── state.py │ │ │ ├ ── graph.py │ │ │ ├ ── nodes/ │ │ │ │ ├ ── parse.py │ │ │ │ ├ ── extract.py │ │ │ │ ├ ── verify.py │ │ │ │ └── validate.py │ │ │ ├ ── parsers/ │ │ │ │ ├ ── pdf.py │ │ │ │ └── docx.py │ │ │ └── prompts.py │ │ ├ ── scanner/ ← ENGINE 2 │ │ │ ├ ── orchestrator.py │ │ │ ├ ── base.py │ │ │ └── providers/ │ │ │ ├ ── compute.py │ │ │ ├ ── kubernetes.py │ │ │ ├ ── storage.py │ │ │ ├ ── database.py │ │ │ ├ ── network.py │ │ │ ├ ── iam.py │ │ │ ├ ── services.py │ │ │ ├ ── security.py │ │ │ └── quotas.py │ │ ├ ── validation/ ← ENGINE 3 │ │ │ ├ ── engine.py │ │ │ └── evaluators.py │ │ └── background/ │ │ ├ ── runner.py │ │ ├ ── extract_task.py │ │ └── scan_task.py │ ├ ── migrations/ │ ├ ── tests/ │ ├ ── pyproject.toml │ ├ ── .env.example │ └── alembic.ini └── frontend/ ├ ── src/ │ ├ ── api/ │ ├ ── components/ │ ├ ── pages/ │ ├ ── hooks/ │ └── types/ └── package.json ``` --- ## PHASE 1 — core/types.py (Build this first, touch nothing else) **Prompt for Claude:** ``` Build backend/app/core/types.py exactly as specified. This file defines ALL data shapes for the entire application. No other file may define Pydantic models. Include in this exact order: 1. All imports: from __future__ import annotations, enum, typing, pydantic, datetime, uuid 2. Enums: CloudProvider (AZURE only), ResourceCategory (RESOURCES/NETWORK/IAM/ SERVICES/SECURITY/QUOTAS), ResourceType (21 types listed below), Severity (CRITICAL/HIGH/MEDIUM/LOW), ResourceIntent (4 values), Operator (8 values), FindingStatus (PASS/FAIL/ERROR/SKIPPED) ResourceType values: virtual_machine, kubernetes_cluster, block_storage, object_storage, relational_database, caching_service, virtual_network, subnet, network_security_group, dns_zone, app_load_balancer, rbac_role_assignment, service_principal, managed_identity, app_registration, enabled_service, private_endpoint, key_vault, certificate, firewall_rule, compute_quota 3. ExtractedCondition and ExtractedRequirement — the LLM output schema. These are FLAT and SIMPLE. All fields are str or Any or float. No Pydantic validators. No enums. Just raw types. ExtractedCondition: attribute(str), operator(str), value(Any), unit(str|None) ExtractedRequirement: resource_type(str), conditions(list[ExtractedCondition]), severity(str), intent(str), source_text(str), confidence(float), reasoning(str) 4. Condition and Prerequisite — the validated form. Condition: attribute(str), operator(Operator), expected_value(Any), unit(str|None), description(str|None) Prerequisite: id(auto), category(ResourceCategory), resource_type(ResourceType), cloud_provider(CloudProvider, default AZURE), intent(ResourceIntent, default MUST_EXIST_BEFORE), conditions(list[Condition]), severity(Severity, default HIGH), source_text(str), confidence(float 0-1), review_required(bool, default False), review_reason(str|None), extracted_at(datetime, auto) 5. All attribute classes — each is a simple BaseModel with all fields Optional: ComputeAttributes: vm_size, min_vcpu(int), min_memory_gb(float), os_type, os_version, architecture, availability_zone KubernetesAttributes: k8s_version, min_node_count(int), max_node_count(int), node_vm_size, network_plugin, rbac_enabled(bool), private_cluster(bool), required_addons(list[str], default []) StorageAttributes: disk_size_gb(int), disk_type, storage_tier, redundancy, encryption_enabled(bool), public_access DatabaseAttributes: db_engine, db_version, db_tier, min_vcores(int), min_storage_gb(int), ha_enabled(bool), backup_retention_days(int), public_access, ssl_required(bool) VirtualNetworkAttributes: vnet_cidr, vnet_exists(bool), region, peering_enabled(bool), dns_servers(list[str], default []) SubnetAttributes: subnet_cidr, min_subnet_size(int), subnet_exists(bool), dedicated_subnet(bool), nsg_attached(bool) NSGAttributes: open_ports(list[int], default []), blocked_ports(list[int], default []), allows_internet_inbound(bool), ssh_rdp_exposed(bool) RBACAttributes: role_name, role_names(list[str], default []), scope, principal_type, role_exists(bool) ServicePrincipalAttributes: sp_exists(bool), sp_type, client_id_exists(bool), client_secret_valid(bool) EnabledServiceAttributes: service_name, service_enabled(bool) EncryptionAttributes: key_vault_exists(bool), encryption_at_rest(bool), encryption_in_transit(bool), tls_version CertificateAttributes: cert_exists(bool), cert_valid(bool), days_until_expiry(int), ssl_enabled(bool) FirewallRuleAttributes: rule_exists(bool), allowed_ip_ranges(list[str], default []), target_service, rule_type ComputeQuotaAttributes: quota_name, vcpu_quota_total(int), vcpu_quota_used(int), vcpu_quota_available(int) 6. ScanResult: resource_id(str), resource_name(str), resource_type(ResourceType), cloud_provider(CloudProvider, default AZURE), region(str), tags(dict, default {}), then one optional field per attribute class (compute, kubernetes, storage, database, virtual_network, subnet, nsg, rbac, service_principal, enabled_service, encryption, certificate, firewall_rule, compute_quota — all default None), raw_response(dict, default {}), scan_timestamp(datetime, auto) 7. Finding and ValidationReport as specified. ValidationReport has a @model_validator(mode="after") called compute_rollup that sets total/passed/failed/errors/skipped from findings list, sets critical_failures and high_failures from failures with matching severity, sets deployment_ready = critical_failures == 0 and high_failures == 0. 8. ATTRIBUTE_TO_GROUP dict — every valid attribute name maps to the ScanResult field name that contains it. Include all attributes from all 14 attribute classes. 9. Three derived lists at the bottom: VALID_ATTRIBUTE_NAMES: list[str] = sorted(ATTRIBUTE_TO_GROUP.keys()) VALID_RESOURCE_TYPES: list[str] = [rt.value for rt in ResourceType] VALID_OPERATORS: list[str] = [op.value for op in Operator] 10. RESOURCE_TYPE_TO_CATEGORY dict mapping each resource_type value to its ResourceCategory. VERIFY: python -c "from app.core.types import *; print('types ok')" must print without error. ``` --- ## PHASE 2 — config.py, database.py, models/, alembic **Prompt for Claude:** ``` Build the database foundation for infra-autopilot. PART A — app/config.py Use pydantic-settings BaseSettings. Read from .env file. Fields: database_url(str), openai_api_key(str), llm_model(str, default "gpt-4.1"), llm_max_tokens(int, default 4000), extraction_confidence_threshold(float, default 0.75), extraction_auto_approve_threshold(float, default 0.90), azure_tenant_id(str), azure_client_id(str), azure_client_secret(str), azure_subscription_id(str), secret_key(str, default "change-me"), log_level(str, default "INFO"). Use @lru_cache on get_settings() so it's read once. PART B — app/database.py Create async SQLAlchemy engine from settings.database_url. pool_size=10, max_overflow=20, pool_pre_ping=True. Create async_sessionmaker with expire_on_commit=False. get_db() is an async generator that yields AsyncSession, commits on exit, rolls back on exception. create_tables() runs metadata.create_all through engine.begin(). PART C — app/models/ (7 files) models/__init__.py — imports all model classes so Alembic autodiscovers them. models/job.py — Job table: id: UUID primary key default gen_random_uuid() job_type: VARCHAR(50) not null — "extraction" or "scan" status: VARCHAR(20) not null default "pending" — pending/running/completed/failed progress_pct: INTEGER default 0 current_step: VARCHAR(100) nullable error_message: TEXT nullable result_id: VARCHAR(100) nullable — ID of the result object when done created_at: TIMESTAMP default now() updated_at: TIMESTAMP default now() onupdate now() models/document.py — two tables: UploadedDocument: id(UUID PK), filename(VARCHAR 255), file_type(VARCHAR 10 — pdf/docx), file_bytes(BYTEA not null — store raw file), content_hash(VARCHAR 64 unique — SHA256), file_size_bytes(INTEGER), created_at(TIMESTAMP default now()) ParsedDocument: id(UUID PK), document_id(UUID FK → uploaded_documents.id), raw_text(TEXT), sections(JSONB — list of {title, text, category}), tables(JSONB — list of {title, columns, rows}), parser_used(VARCHAR 50), parse_errors(JSONB — list of error strings), created_at(TIMESTAMP default now()) models/prerequisite.py — Prerequisite table: id: VARCHAR(20) PK — matches pre_{uuid} format from types.py document_id: UUID FK → uploaded_documents.id extraction_job_id: UUID FK → jobs.id nullable category: VARCHAR(20) not null resource_type: VARCHAR(50) not null cloud_provider: VARCHAR(10) default "azure" intent: VARCHAR(30) not null severity: VARCHAR(10) not null conditions: JSONB not null — serialized list of Condition dicts source_text: TEXT not null confidence: FLOAT not null review_required: BOOLEAN default false review_reason: TEXT nullable extracted_at: TIMESTAMP default now() models/scan_result.py — ScanResult table: id: UUID PK scan_job_id: UUID FK → jobs.id resource_id: TEXT not null — Azure ARM ID resource_name: VARCHAR(255) resource_type: VARCHAR(50) not null cloud_provider: VARCHAR(10) default "azure" region: VARCHAR(50) tags: JSONB default {} attributes: JSONB not null — the populated attribute group as dict attribute_group: VARCHAR(50) not null — which group: compute/kubernetes/etc. raw_response: JSONB default {} scan_timestamp: TIMESTAMP default now() models/report.py — two tables: ValidationReport: id(VARCHAR 20 PK matching rpt_{uuid}), scan_job_id(UUID FK), cloud_provider(VARCHAR 10), region(VARCHAR 50), total(INTEGER), passed(INTEGER), failed(INTEGER), errors(INTEGER), skipped(INTEGER), critical_failures(INTEGER), high_failures(INTEGER), deployment_ready(BOOLEAN), generated_at(TIMESTAMP) Finding: id(UUID PK), report_id(VARCHAR 20 FK → validation_reports.id), prerequisite_id(VARCHAR 20), resource_id(TEXT nullable), resource_name(VARCHAR 255 nullable), condition(JSONB not null — serialized Condition dict), status(VARCHAR 10), severity(VARCHAR 10), actual_value(JSONB nullable), expected_value(JSONB nullable), reason(TEXT nullable) models/review.py — ReviewQueueItem table: id: UUID PK prerequisite_id: VARCHAR(20) FK → prerequisites.id reason: TEXT not null resolved: BOOLEAN default false resolution: VARCHAR(20) nullable — approved/rejected/modified reviewer_note: TEXT nullable created_at: TIMESTAMP default now() resolved_at: TIMESTAMP nullable PART D — Alembic setup alembic.ini — set script_location = migrations, sqlalchemy.url left blank (use env.py) migrations/env.py — import Base from app.database, import all models via app.models, set target_metadata = Base.metadata, read database_url from settings for both online and offline modes. Use asyncio run_sync for async engine. PART E — Run this to create first migration: alembic revision --autogenerate -m "initial_schema" alembic upgrade head VERIFY: python -c "from app.models import *; print('models ok')" alembic current # should show head ``` --- ## PHASE 3 — extraction/prompts.py and extraction/parsers/ **Prompt for Claude:** ``` Build the document parsers and LLM prompts for infra-autopilot. PART A — extraction/prompts.py This file has two things: the system prompt and a function to inject context. SYSTEM_PROMPT is a multi-line string. It must contain exactly this content: You are an infrastructure requirements extractor. Your job is to read deployment documentation and extract every concrete, checkable infrastructure prerequisite. RULES: - Only extract requirements that can be objectively verified by checking a cloud resource. - Do not extract general advice, recommendations, or documentation instructions. - Every condition attribute MUST be one of the valid attribute names listed below. - Every operator MUST be one of the valid operators listed below. - Set confidence based on how clearly the document states the requirement: 1.0 = explicitly stated with exact values ("must be Kubernetes 1.28 or higher") 0.9 = clearly stated but slightly ambiguous ("recent Kubernetes version required") 0.75 = implied but not directly stated ("production-grade cluster") below 0.75 = unclear — extract anyway but explain your uncertainty in reasoning VALID ATTRIBUTE NAMES (you must use ONLY these, no others): {attribute_names} VALID RESOURCE TYPES (you must use ONLY these, no others): {resource_types} VALID OPERATORS: gte = actual &gt;= expected (numbers and version strings) lte = actual &lt;= expected (numbers and version strings) eq = exact match (strings, booleans) in = actual is one of expected list exists = resource/field must exist (no expected_value needed, set value to null) contains = list contains value, or string contains substring regex = matches regex pattern not_eq = must not equal OPERATOR SELECTION GUIDE: - "must be at least X" or "minimum X" or "&gt;= X" → gte - "must not exceed X" or "maximum X" → lte - "must be X" or "must equal X" or "required: X" → eq - "must be one of X, Y, Z" → in - "must exist" or "must be present" or "required" (no value) → exists - "must include X" or "must have X in list" → contains - boolean fields like ssl_required, ha_enabled, private_cluster → eq with true/false OUTPUT FORMAT: Return a JSON object with a single key "requirements" containing a list. Each item in the list is an ExtractedRequirement with these exact fields: { "resource_type": "one of the valid resource types", "conditions": [ {"attribute": "valid_attribute_name", "operator": "valid_operator", "value": &lt;value or null&gt;, "unit": null} ], "severity": "critical | high | medium | low", "intent": "must_exist_before | will_be_created | optional", "source_text": "verbatim sentence or table row from the document", "confidence": 0.0-1.0, "reasoning": "brief explanation of why you extracted this and how confident you are" } IMPORTANT: Return ONLY the JSON. No markdown. No explanation. No backticks. The file also exports: def build_user_message(text: str, tables: list[dict]) -&gt; str: """Build the user message from document text and tables.""" parts = ["DOCUMENT TEXT:\n", text[:8000]] # hard limit if tables: parts.append("\n\nTABLES FROM DOCUMENT:") for i, table in enumerate(tables[:5]): # max 5 tables parts.append(f"\nTable {i+1}:") if "rows" in table and table["rows"]: headers = table.get("columns", []) parts.append(" | ".join(str(h) for h in headers)) parts.append("-" * 40) for row in table["rows"][:20]: # max 20 rows per table parts.append(" | ".join(str(v) for v in row.values())) return "\n".join(parts) def get_formatted_prompt() -&gt; str: """Return SYSTEM_PROMPT with attribute names and resource types injected.""" from app.core.types import VALID_ATTRIBUTE_NAMES, VALID_RESOURCE_TYPES return SYSTEM_PROMPT.format( attribute_names="\n".join(f" {name}" for name in VALID_ATTRIBUTE_NAMES), resource_types="\n".join(f" {rt}" for rt in VALID_RESOURCE_TYPES), ) PART B — extraction/parsers/pdf.py class PDFParser: def parse(self, file_bytes: bytes) -&gt; dict: """ Returns: { "raw_text": str, "sections": list[{"title": str, "text": str}], "tables": list[{"title": str, "columns": list, "rows": list[dict]}], "parser_used": "docling" | "pdfplumber", "errors": list[str] } Try docling first. On any exception, fall back to pdfplumber. Never raises. Always returns a dict. """ For docling: from docling.document_converter import DocumentConverter import tempfile, os Write file_bytes to a temp file (docling needs a file path, not bytes). converter = DocumentConverter() result = converter.convert(temp_path) doc = result.document Extract raw_text from doc.export_to_markdown() — this gives clean text. For tables: iterate doc.tables, each has .data which is a 2D list. First row is headers. Remaining rows are data. Convert to {"title": f"Table {i}", "columns": headers, "rows": list of dicts} For sections: split raw_text by "## " or "# " headings. Each section is {"title": heading_text, "text": section_body} Clean up temp file in finally block. For pdfplumber fallback: import pdfplumber, io with pdfplumber.open(io.BytesIO(file_bytes)) as pdf: all_text = "" all_tables = [] for page in pdf.pages: all_text += page.extract_text() or "" for table in page.extract_tables() or []: if table and len(table) &gt; 1: headers = [str(h) for h in (table[0] or [])] rows = [] for row in table[1:]: if row: rows.append({ headers[i]: str(v) if v else "" for i, v in enumerate(row) if i &lt; len(headers) }) all_tables.append({"title": f"Table {len(all_tables)+1}", "columns": headers, "rows": rows}) Return with parser_used="pdfplumber" PART C — extraction/parsers/docx.py class DocxParser: def parse(self, file_bytes: bytes) -&gt; dict: """Same return shape as PDFParser.parse()""" from docx import Document import io doc = Document(io.BytesIO(file_bytes)) raw_text = "" sections = [] tables = [] current_section_title = "Introduction" current_section_text = [] For each paragraph in doc.paragraphs: If style starts with "Heading": Save current section (if has text), start new section current_section_title = paragraph.text Else: raw_text += paragraph.text + "\n" current_section_text.append(paragraph.text) Save last section. For each table in doc.tables: First row = headers (cell.text for cell in row.cells) Remaining rows = data rows Append to tables list Return dict with parser_used="python-docx", errors=[] Never raises. Wrap everything in try/except, return partial results with errors. VERIFY: from app.extraction.parsers.pdf import PDFParser from app.extraction.parsers.docx import DocxParser print("parsers ok") ``` --- ## PHASE 4 — LangGraph extraction pipeline **Prompt for Claude:** ``` Build the LangGraph extraction pipeline for infra-autopilot. This is ENGINE 1: Document bytes → list[Prerequisite] PART A — extraction/state.py ExtractionState is a TypedDict with these fields: # Input — set before graph starts document_id: str file_bytes: bytes file_type: str # "pdf" or "docx" # After parse_node raw_text: str sections: list[dict] tables: list[dict] parser_used: str parse_errors: list[str] # After extract_node raw_requirements: list[dict] # raw JSON from LLM before any mapping extract_attempts: int # retry counter, starts 0 # After verify_node requirements_with_flags: list[dict] # requirements with review_required added # After validate_node prerequisites: list[dict] # final validated, ready to save invalid_requirements: list[dict] # failed validation, go to review queue # Error tracking errors: list[str] completed: bool PART B — extraction/nodes/parse.py async def parse_node(state: ExtractionState) -&gt; dict: """ Parses document bytes into text and tables. Returns partial state update (only keys that changed). Never raises — all errors go to state["errors"]. """ try: if state["file_type"] == "pdf": from app.extraction.parsers.pdf import PDFParser result = PDFParser().parse(state["file_bytes"]) else: from app.extraction.parsers.docx import DocxParser result = DocxParser().parse(state["file_bytes"]) return { "raw_text": result["raw_text"], "sections": result["sections"], "tables": result["tables"], "parser_used": result["parser_used"], "parse_errors": result.get("errors", []), } except Exception as e: log.error("parse_node_failed", error=str(e)) return { "raw_text": "", "sections": [], "tables": [], "parser_used": "failed", "parse_errors": [str(e)], "errors": state.get("errors", []) + [f"parse_failed: {e}"], } PART C — extraction/nodes/extract.py from app.extraction.prompts import get_formatted_prompt, build_user_message from app.core.types import ExtractedRequirement from app.config import get_settings import instructor import openai import json class ExtractionOutput(BaseModel): requirements: list[ExtractedRequirement] async def extract_node(state: ExtractionState) -&gt; dict: """ Calls GPT-4.1 via instructor to extract requirements. On failure, increments extract_attempts. Returns empty requirements on repeated failure (not an exception). """ settings = get_settings() if not state.get("raw_text") and not state.get("tables"): log.warning("extract_node_no_content") return {"raw_requirements": [], "extract_attempts": state.get("extract_attempts", 0)} client = instructor.from_openai(openai.AsyncOpenAI(api_key=settings.openai_api_key)) try: result = await client.chat.completions.create( model=settings.llm_model, max_tokens=settings.llm_max_tokens, response_model=ExtractionOutput, messages=[ {"role": "system", "content": get_formatted_prompt()}, {"role": "user", "content": build_user_message( state.get("raw_text", ""), state.get("tables", []) )}, ], max_retries=2, # instructor internal retry for schema enforcement ) raw = [r.model_dump() for r in result.requirements] log.info("extract_node_complete", count=len(raw)) return { "raw_requirements": raw, "extract_attempts": state.get("extract_attempts", 0), } except Exception as e: attempts = state.get("extract_attempts", 0) + 1 log.error("extract_node_failed", attempt=attempts, error=str(e)) return { "raw_requirements": [], "extract_attempts": attempts, "errors": state.get("errors", []) + [f"extract_failed_attempt_{attempts}: {e}"], } PART D — extraction/nodes/verify.py from app.config import get_settings async def verify_node(state: ExtractionState) -&gt; dict: """ Checks confidence of each requirement. Sets review_required based on thresholds from settings. Does NOT call the LLM again — confidence flagging only. Flags: confidence &gt;= auto_approve_threshold → review_required=False confidence &gt;= confidence_threshold and &lt; auto_approve_threshold → review_required=False (approved but noted) confidence &lt; confidence_threshold → review_required=True intent == "will_be_created" → review_required=True always """ settings = get_settings() auto_approve = settings.extraction_auto_approve_threshold flag_threshold = settings.extraction_confidence_threshold flagged = [] for req in state.get("raw_requirements", []): r = dict(req) confidence = r.get("confidence", 0.0) intent = r.get("intent", "must_exist_before") if intent == "will_be_created": r["review_required"] = True r["review_reason"] = "intent is will_be_created — verify Terraform/IaC handles this" elif confidence &lt; flag_threshold: r["review_required"] = True r["review_reason"] = f"confidence {confidence:.2f} below threshold {flag_threshold}" else: r["review_required"] = False r["review_reason"] = None flagged.append(r) log.info("verify_node_complete", total=len(flagged), for_review=sum(1 for r in flagged if r["review_required"])) return {"requirements_with_flags": flagged} PART E — extraction/nodes/validate.py from app.core.types import ( ATTRIBUTE_TO_GROUP, VALID_ATTRIBUTE_NAMES, VALID_RESOURCE_TYPES, VALID_OPERATORS, RESOURCE_TYPE_TO_CATEGORY, Prerequisite, Condition, ResourceType, ResourceCategory, ResourceIntent, Severity, Operator, CloudProvider, ) from datetime import datetime import uuid def _map_to_prerequisite(req: dict) -&gt; Prerequisite | None: """ Converts a raw requirement dict (with review_required flag) to a Prerequisite. Returns None if the requirement is fundamentally invalid (wrong resource_type, all conditions invalid). Returns Prerequisite with review_required=True if any condition attribute is invalid (maps to review queue). """ # Validate resource_type resource_type_str = req.get("resource_type", "") if resource_type_str not in VALID_RESOURCE_TYPES: log.warning("invalid_resource_type", value=resource_type_str) return None resource_type = ResourceType(resource_type_str) category = RESOURCE_TYPE_TO_CATEGORY.get(resource_type_str, ResourceCategory.RESOURCES) # Validate and convert conditions valid_conditions = [] invalid_attrs = [] for cond in req.get("conditions", []): attr = cond.get("attribute", "") op_str = cond.get("operator", "") if attr not in VALID_ATTRIBUTE_NAMES: invalid_attrs.append(attr) continue if op_str not in VALID_OPERATORS: invalid_attrs.append(f"bad_operator:{op_str}") continue valid_conditions.append(Condition( attribute=attr, operator=Operator(op_str), expected_value=cond.get("value"), unit=cond.get("unit"), )) if not valid_conditions: log.warning("no_valid_conditions", resource_type=resource_type_str, invalid_attrs=invalid_attrs) return None # Map severity and intent (with safe fallbacks) severity_map = {"critical": Severity.CRITICAL, "high": Severity.HIGH, "medium": Severity.MEDIUM, "low": Severity.LOW} intent_map = {"must_exist_before": ResourceIntent.MUST_EXIST_BEFORE, "will_be_created": ResourceIntent.WILL_BE_CREATED, "must_not_exist": ResourceIntent.MUST_NOT_EXIST, "optional": ResourceIntent.OPTIONAL} severity = severity_map.get(req.get("severity", "high"), Severity.HIGH) intent = intent_map.get(req.get("intent", "must_exist_before"), ResourceIntent.MUST_EXIST_BEFORE) review_required = req.get("review_required", False) review_reason = req.get("review_reason") if invalid_attrs: review_required = True review_reason = (review_reason or "") + f" Invalid attributes removed: {invalid_attrs}" return Prerequisite( category=category, resource_type=resource_type, intent=intent, conditions=valid_conditions, severity=severity, source_text=req.get("source_text", ""), confidence=float(req.get("confidence", 0.5)), review_required=review_required, review_reason=review_reason, ) async def validate_node(state: ExtractionState) -&gt; dict: """ Maps raw requirements to Prerequisite objects. Splits into prerequisites (valid) and invalid_requirements (failed mapping). """ prerequisites = [] invalid_requirements = [] for req in state.get("requirements_with_flags", []): try: prereq = _map_to_prerequisite(req) if prereq is None: invalid_requirements.append({**req, "failure_reason": "mapping_failed"}) else: prerequisites.append(prereq.model_dump(mode="json")) except Exception as e: log.error("validate_node_item_failed", error=str(e)) invalid_requirements.append({**req, "failure_reason": str(e)}) log.info("validate_node_complete", valid=len(prerequisites), invalid=len(invalid_requirements)) return { "prerequisites": prerequisites, "invalid_requirements": invalid_requirements, "completed": True, } PART F — extraction/graph.py from langgraph.graph import StateGraph, END from app.extraction.state import ExtractionState from app.extraction.nodes.parse import parse_node from app.extraction.nodes.extract import extract_node from app.extraction.nodes.verify import verify_node from app.extraction.nodes.validate import validate_node def should_retry_extract(state: ExtractionState) -&gt; str: """ Routing function after extract_node. If extraction returned nothing AND we haven't tried twice, retry. Otherwise proceed to verify. """ if (not state.get("raw_requirements") and state.get("extract_attempts", 0) &lt; 2 and state.get("raw_text")): return "retry" return "verify" def build_graph(): graph = StateGraph(ExtractionState) graph.add_node("parse", parse_node) graph.add_node("extract", extract_node) graph.add_node("verify", verify_node) graph.add_node("validate", validate_node) graph.set_entry_point("parse") graph.add_edge("parse", "extract") graph.add_conditional_edges( "extract", should_retry_extract, {"retry": "extract", "verify": "verify"} ) graph.add_edge("verify", "validate") graph.add_edge("validate", END) return graph.compile() # Module-level compiled graph — built once, reused extraction_graph = build_graph() VERIFY: from app.extraction.graph import extraction_graph print(extraction_graph.get_graph().print_ascii()) ``` --- ## PHASE 5 — scanner/base.py, orchestrator.py, all providers **Prompt for Claude:** ``` Build ENGINE 2: the Azure scanner for infra-autopilot. Input: list[Prerequisite dicts] + scan scope Output: list[ScanResult] PART A — scanner/base.py from abc import ABC, abstractmethod from app.core.types import ScanResult class BaseProvider(ABC): @abstractmethod def get_all(self, resource_group: str | None = None, name_filter: str | None = None) -&gt; list[ScanResult]: """Never raises. Returns [] on failure.""" ... @abstractmethod def get_by_id(self, resource_id: str) -&gt; ScanResult | None: """Returns None if not found or on error.""" ... All Azure providers inherit from this. Constructor always takes (credential: TokenCredential, subscription_id: str). All methods catch all exceptions, log with structlog, return empty/None. PART B — scanner/providers/compute.py AzureComputeProvider(BaseProvider): Uses azure-mgmt-compute: ComputeManagementClient __init__: self._client = ComputeManagementClient(credential, subscription_id) self._sku_cache: dict[str, dict[str, dict]] = {} get_all(resource_group, name_filter) -&gt; list[ScanResult]: If resource_group: iterator = self._client.virtual_machines.list(resource_group) Else: iterator = self._client.virtual_machines.list_all() For each vm: filter by name_filter if set, call _adapt(vm), append Return results. Never raise. get_by_id(resource_id) -&gt; ScanResult | None: Parse ARM ID with _parse_arm_id(resource_id) → (rg, name) Call self._client.virtual_machines.get(rg, name) Return _adapt(result) _warm_sku_cache(location: str): If location already in self._sku_cache: return self._sku_cache[location] = {} for sku in self._client.resource_skus.list(filter=f"location eq '{location}'"): if sku.name and sku.capabilities: self._sku_cache[location][sku.name] = {c.name: c.value for c in sku.capabilities} _adapt(raw_vm) -&gt; ScanResult: location = raw_vm.location or "" vm_size = raw_vm.hardware_profile.vm_size if raw_vm.hardware_profile else None min_vcpu, min_memory_gb = None, None if vm_size and location: self._warm_sku_cache(location) caps = self._sku_cache.get(location, {}).get(vm_size, {}) if "vCPUs" in caps: min_vcpu = int(caps["vCPUs"]) if "MemoryGB" in caps: min_memory_gb = float(caps["MemoryGB"]) os_type, os_version = None, None if raw_vm.storage_profile: if raw_vm.storage_profile.os_disk: os_type = raw_vm.storage_profile.os_disk.os_type if raw_vm.storage_profile.image_reference: ref = raw_vm.storage_profile.image_reference os_version = " ".join(p for p in [ref.offer, ref.sku] if p) or None az = raw_vm.zones[0] if raw_vm.zones else None from app.core.types import ComputeAttributes, ResourceType, CloudProvider return ScanResult( resource_id=raw_vm.id or "", resource_name=raw_vm.name or "", resource_type=ResourceType.VIRTUAL_MACHINE, cloud_provider=CloudProvider.AZURE, region=location, tags=dict(raw_vm.tags or {}), compute=ComputeAttributes(vm_size=vm_size, min_vcpu=min_vcpu, min_memory_gb=min_memory_gb, os_type=os_type, os_version=os_version, availability_zone=az), raw_response=raw_vm.as_dict() if hasattr(raw_vm, "as_dict") else {}, ) PART C — scanner/providers/kubernetes.py AzureKubernetesProvider(BaseProvider): Uses azure-mgmt-containerservice: ContainerServiceClient _adapt(raw_cluster) -&gt; ScanResult: k8s_version = raw_cluster.kubernetes_version min_node, max_node, node_vm_size = None, None, None if raw_cluster.agent_pool_profiles: for pool in raw_cluster.agent_pool_profiles: if pool.min_count is not None: min_node = min(min_node, pool.min_count) if min_node is not None else pool.min_count if pool.max_count is not None: max_node = max(max_node, pool.max_count) if max_node is not None else pool.max_count if getattr(pool, "mode", None) == "System" and pool.vm_size: node_vm_size = pool.vm_size network_plugin = None if raw_cluster.network_profile: network_plugin = raw_cluster.network_profile.network_plugin private_cluster = False if raw_cluster.api_server_access_profile: private_cluster = bool(raw_cluster.api_server_access_profile.enable_private_cluster) addons = [] if raw_cluster.addon_profiles: for name, profile in raw_cluster.addon_profiles.items(): if getattr(profile, "enabled", False): addons.append(name) return ScanResult( resource_id=raw_cluster.id or "", resource_name=raw_cluster.name or "", resource_type=ResourceType.KUBERNETES_CLUSTER, cloud_provider=CloudProvider.AZURE, region=raw_cluster.location or "", tags=dict(raw_cluster.tags or {}), kubernetes=KubernetesAttributes( k8s_version=k8s_version, min_node_count=min_node, max_node_count=max_node, node_vm_size=node_vm_size, network_plugin=network_plugin, rbac_enabled=raw_cluster.enable_rbac, private_cluster=private_cluster, required_addons=addons, ), raw_response=raw_cluster.as_dict() if hasattr(raw_cluster, "as_dict") else {}, ) PART D — scanner/providers/storage.py AzureStorageProvider(BaseProvider): Uses azure-mgmt-storage: StorageManagementClient _adapt(raw_account) -&gt; ScanResult: tier = getattr(raw_account, "access_tier", None) redundancy = None if raw_account.sku and raw_account.sku.name: parts = raw_account.sku.name.split("_") redundancy = parts[1] if len(parts) == 2 else None public_access = "disabled" if getattr(raw_account, "allow_blob_public_access", False): public_access = "enabled" return ScanResult( resource_id=raw_account.id or "", resource_name=raw_account.name or "", resource_type=ResourceType.OBJECT_STORAGE, cloud_provider=CloudProvider.AZURE, region=raw_account.location or "", tags=dict(raw_account.tags or {}), storage=StorageAttributes(storage_tier=tier, redundancy=redundancy, encryption_enabled=True, public_access=public_access), raw_response=raw_account.as_dict() if hasattr(raw_account, "as_dict") else {}, ) PART E — scanner/providers/database.py AzureDatabaseProvider(BaseProvider): Uses azure-mgmt-rdbms: PostgreSQLManagementClient (for Flexible Server) Import: from azure.mgmt.rdbms.postgresql_flexibleservers import PostgreSQLManagementClient get_all returns PostgreSQL servers only for now. _adapt(raw_server) -&gt; ScanResult: ha_enabled = False if raw_server.high_availability: ha_enabled = raw_server.high_availability.mode not in (None, "Disabled") backup_days = None if raw_server.backup: backup_days = raw_server.backup.backup_retention_days public_access = "disabled" if raw_server.network and getattr(raw_server.network, "public_network_access", None) == "Enabled": public_access = "enabled" db_tier, min_vcores = None, None if raw_server.sku: db_tier = raw_server.sku.tier if hasattr(raw_server.sku, "capacity") and raw_server.sku.capacity: min_vcores = int(raw_server.sku.capacity) min_storage = None if raw_server.storage: min_storage = raw_server.storage.storage_size_gb return ScanResult( resource_id=raw_server.id or "", resource_name=raw_server.name or "", resource_type=ResourceType.RELATIONAL_DATABASE, cloud_provider=CloudProvider.AZURE, region=raw_server.location or "", tags=dict(raw_server.tags or {}), database=DatabaseAttributes( db_engine="postgresql", db_version=raw_server.version, db_tier=db_tier, min_vcores=min_vcores, min_storage_gb=min_storage, ha_enabled=ha_enabled, backup_retention_days=backup_days, public_access=public_access, ssl_required=True, ), raw_response=raw_server.as_dict() if hasattr(raw_server, "as_dict") else {}, ) PART F — scanner/providers/network.py AzureNetworkProvider — has separate methods (not just get_all/get_by_id): Uses azure-mgmt-network: NetworkManagementClient get_all_vnets(resource_group, name_filter) → list[ScanResult] using virtual_networks.list_all() or .list(rg) get_all_subnets(resource_group) → list[ScanResult] Must iterate all vnets first, then subnets.list(rg, vnet_name) per vnet. Subnets inherit location from parent VNet. min_subnet_size = ipaddress.ip_network(cidr).num_addresses - 5 get_all_nsgs(resource_group, name_filter) → list[ScanResult] Parse security_rules: open_ports = inbound Allow rules with single port numbers ssh_rdp_exposed = port 22 or 3389 in open_ports AND source_address_prefix is "*" or "Internet" allows_internet_inbound = any rule with destination_port_range="*" and access="Allow" and direction="Inbound" Adapters: _adapt_vnet(raw_vnet) → ScanResult with virtual_network=VirtualNetworkAttributes( vnet_cidr=address_space.address_prefixes[0], vnet_exists=True, region=raw_vnet.location, peering_enabled=bool(raw_vnet.virtual_network_peerings), dns_servers=list(raw_vnet.dhcp_options.dns_servers) if raw_vnet.dhcp_options else []) _adapt_subnet(raw_subnet, location) → ScanResult with subnet=SubnetAttributes(...) _adapt_nsg(raw_nsg) → ScanResult with nsg=NSGAttributes(...) get_all() routes to get_all_vnets() (for compatibility with orchestrator). PART G — scanner/providers/iam.py (RBAC only — skip Graph API) AzureIAMProvider(BaseProvider): Uses azure-mgmt-authorization: AuthorizationManagementClient Uses azure-mgmt-msi: ManagedServiceIdentityClient _role_name_cache: dict[str, str] = {} _build_role_cache(): If cache already populated: return for role_def in self._auth_client.role_definitions.list(f"/subscriptions/{subscription_id}"): if role_def.id: self._role_name_cache[role_def.id] = role_def.role_name get_all_role_assignments(scope=None) → list[ScanResult]: self._build_role_cache() filter_str = f"atScope()" — lists all assignments in subscription for assignment in self._auth_client.role_assignments.list_for_subscription(): role_name = self._role_name_cache.get(assignment.properties.role_definition_id, "Unknown") scope = assignment.properties.scope or "" scope_level = "resource_group" if "resourcegroups" in scope.lower() else "subscription" principal_type_map = {"User":"user","ServicePrincipal":"service_principal","Group":"group"} principal_type = principal_type_map.get(assignment.properties.principal_type or "", None) yield ScanResult( resource_id=assignment.id, resource_name=role_name, resource_type=ResourceType.RBAC_ROLE_ASSIGNMENT, cloud_provider=CloudProvider.AZURE, region="global", rbac=RBACAttributes(role_name=role_name, role_names=[role_name], scope=scope_level, principal_type=principal_type, role_exists=True), raw_response={"id": assignment.id, "scope": scope, "principalType": assignment.properties.principal_type} ) get_all() routes to get_all_role_assignments() PART H — scanner/providers/services.py AzureServicesProvider(BaseProvider): Uses azure-mgmt-resource: ResourceManagementClient get_all_enabled_services() → list[ScanResult]: for provider in self._resource_client.providers.list(): yield ScanResult( resource_id=provider.namespace, resource_name=provider.namespace, resource_type=ResourceType.ENABLED_SERVICE, cloud_provider=CloudProvider.AZURE, region="global", enabled_service=EnabledServiceAttributes( service_name=provider.namespace, service_enabled=provider.registration_state == "Registered"), raw_response={"namespace": provider.namespace, "registrationState": provider.registration_state} ) is_service_enabled(service_name: str) → bool: provider = self._resource_client.providers.get(service_name) return provider.registration_state == "Registered" get_all() routes to get_all_enabled_services() PART I — scanner/providers/security.py AzureSecurityProvider(BaseProvider): Uses azure-mgmt-keyvault: KeyVaultManagementClient Uses azure-keyvault-certificates: CertificateClient (for cert details) get_all_key_vaults(resource_group=None) → list[ScanResult]: iterator = self._kv_client.vaults.list_by_subscription() if not resource_group else self._kv_client.vaults.list_by_resource_group(resource_group) for vault: _adapt_key_vault(vault) _adapt_key_vault(raw_vault) → ScanResult: ScanResult(..., resource_type=ResourceType.KEY_VAULT, encryption=EncryptionAttributes(key_vault_exists=True), raw_response=raw_vault.as_dict()) get_certificates_in_vault(vault_name, vault_url) → list[ScanResult]: from azure.keyvault.certificates import CertificateClient cert_client = CertificateClient(vault_url=vault_url, credential=self._credential) from datetime import datetime, timezone now = datetime.now(timezone.utc) for cert in cert_client.list_properties_of_certificates(): expires = cert.expires_on days = (expires - now).days if expires else None valid = days is not None and days &gt; 0 yield ScanResult(..., resource_type=ResourceType.CERTIFICATE, certificate=CertificateAttributes(cert_exists=True, cert_valid=valid, days_until_expiry=days, ssl_enabled=True), raw_response={"name": cert.name, "vault": vault_url}) get_all() routes to get_all_key_vaults() PART J — scanner/providers/quotas.py AzureQuotasProvider(BaseProvider): Uses azure-mgmt-compute: ComputeManagementClient (for .usage.list) get_compute_quotas(region: str) → list[ScanResult]: for usage in self._compute_client.usage.list(location=region): total = usage.limit used = usage.current_value available = (total - used) if total is not None and used is not None else None yield ScanResult( resource_id=f"quota/compute/{region}/{usage.name.value}", resource_name=usage.name.localized_value or usage.name.value or "", resource_type=ResourceType.COMPUTE_QUOTA, cloud_provider=CloudProvider.AZURE, region=region, compute_quota=ComputeQuotaAttributes(quota_name=usage.name.value, vcpu_quota_total=total, vcpu_quota_used=used, vcpu_quota_available=available), raw_response={"name": usage.name.value, "limit": usage.limit, "currentValue": usage.current_value} ) get_all() with no region → use "eastus" as default PART K — scanner/orchestrator.py class ScanOrchestrator: def __init__(self, subscription_id: str, tenant_id: str, client_id: str, client_secret: str): from azure.identity import ClientSecretCredential self._credential = ClientSecretCredential(tenant_id, client_id, client_secret) self._subscription_id = subscription_id self._compute = AzureComputeProvider(self._credential, subscription_id) self._kubernetes = AzureKubernetesProvider(self._credential, subscription_id) self._storage = AzureStorageProvider(self._credential, subscription_id) self._database = AzureDatabaseProvider(self._credential, subscription_id) self._network = AzureNetworkProvider(self._credential, subscription_id) self._iam = AzureIAMProvider(self._credential, subscription_id) self._services = AzureServicesProvider(self._credential, subscription_id) self._security = AzureSecurityProvider(self._credential, subscription_id) self._quotas = AzureQuotasProvider(self._credential, subscription_id) def run(self, prerequisites: list[dict], resource_group: str | None = None, region: str = "eastus") -&gt; list[ScanResult]: """ Main entry point. Takes prerequisite dicts (from DB), returns ScanResults. 1. Determine which resource types to scan (from prerequisites, skip WILL_BE_CREATED) 2. Map resource types to providers 3. Run each provider in a ThreadPoolExecutor (Azure SDK is synchronous) 4. Collect and return all results Never raises. Returns partial results on provider failure. """ from app.core.types import ResourceIntent, ResourceType types_to_scan = set() for prereq in prerequisites: if prereq.get("intent") != ResourceIntent.WILL_BE_CREATED.value: types_to_scan.add(prereq.get("resource_type")) PROVIDER_MAP = { "virtual_machine": lambda: self._compute.get_all(resource_group), "kubernetes_cluster": lambda: self._kubernetes.get_all(resource_group), "object_storage": lambda: self._storage.get_all(resource_group), "block_storage": lambda: self._storage.get_all(resource_group), "relational_database": lambda: self._database.get_all(resource_group), "virtual_network": lambda: self._network.get_all_vnets(resource_group), "subnet": lambda: self._network.get_all_subnets(resource_group), "network_security_group": lambda: self._network.get_all_nsgs(resource_group), "rbac_role_assignment": lambda: self._iam.get_all(), "service_principal": lambda: self._iam.get_all(), "enabled_service": lambda: self._services.get_all(), "key_vault": lambda: self._security.get_all(), "certificate": lambda: self._security.get_all(), "compute_quota": lambda: self._quotas.get_compute_quotas(region), } from concurrent.futures import ThreadPoolExecutor, as_completed all_results = [] handlers_to_run = {rt: PROVIDER_MAP[rt] for rt in types_to_scan if rt in PROVIDER_MAP} with ThreadPoolExecutor(max_workers=8) as executor: future_to_type = {executor.submit(handler): rt for rt, handler in handlers_to_run.items()} for future in as_completed(future_to_type): rt = future_to_type[future] try: results = future.result() all_results.extend(results) log.info("provider_complete", resource_type=rt, count=len(results)) except Exception as e: log.error("provider_failed", resource_type=rt, error=str(e)) return all_results ``` --- ## PHASE 6 — validation/evaluators.py and validation/engine.py **Prompt for Claude:** ``` Build ENGINE 3: the validation engine for infra-autopilot. Input: list[Prerequisite dicts] + list[ScanResult dicts] Output: ValidationReport PART A — validation/evaluators.py One function per Operator. Each takes (actual: Any, expected: Any) → bool. None actual always returns False except for EXISTS. All functions catch exceptions and return False. def evaluate_gte(actual, expected) → bool: Compare as floats if both numeric, else as version strings using packaging.version. Return False if comparison fails. Try numeric first: return float(actual) &gt;= float(expected) On ValueError, try version comparison: from packaging.version import Version return Version(str(actual)) &gt;= Version(str(expected)) On any exception: return False def evaluate_lte(actual, expected) → bool: Mirror of gte. def evaluate_eq(actual, expected) → bool: If both are bool: return bool(actual) == bool(expected) String comparison case-insensitive: str(actual).strip().lower() == str(expected).strip().lower() def evaluate_in(actual, expected) → bool: expected must be a list. Convert to list if not. Check str(actual).lower() in [str(e).lower() for e in expected_list] def evaluate_exists(actual, expected) → bool: Returns True if actual is not None and not empty string. Ignores expected entirely. def evaluate_contains(actual, expected) → bool: If actual is a list: check expected in actual (with str lowercasing) If actual is a string: check str(expected).lower() in str(actual).lower() def evaluate_not_eq(actual, expected) → bool: return not evaluate_eq(actual, expected) def evaluate_regex(actual, expected) → bool: import re return bool(re.search(str(expected), str(actual))) EVALUATOR_MAP = { "gte": evaluate_gte, "lte": evaluate_lte, "eq": evaluate_eq, "in": evaluate_in, "exists": evaluate_exists, "contains": evaluate_contains, "not_eq": evaluate_not_eq, "regex": evaluate_regex, } PART B — validation/engine.py class ValidationEngine: def run(self, prerequisites: list[dict], scan_results: list[dict], scan_job_id: str, region: str = "eastus") → ValidationReport: """ Entry point. Both inputs are plain dicts (from DB / JSON). Converts internally to typed objects for evaluation. Returns ValidationReport (typed). """ from app.core.types import (Prerequisite, ScanResult, Finding, ValidationReport, ResourceIntent, FindingStatus, Severity, ATTRIBUTE_TO_GROUP, CloudProvider) from app.validation.evaluators import EVALUATOR_MAP # Convert dicts to typed objects typed_prereqs = [Prerequisite.model_validate(p) for p in prerequisites] typed_results = [ScanResult.model_validate(r) for r in scan_results] # Index scan results by resource_type for O(1) lookup results_by_type: dict[str, list[ScanResult]] = {} for result in typed_results: key = result.resource_type.value results_by_type.setdefault(key, []).append(result) findings = [] for prereq in typed_prereqs: # Skip WILL_BE_CREATED if prereq.intent == ResourceIntent.WILL_BE_CREATED: for condition in prereq.conditions: findings.append(Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.SKIPPED, severity=prereq.severity, reason="intent is WILL_BE_CREATED — not scanned" )) continue # MUST_NOT_EXIST if prereq.intent == ResourceIntent.MUST_NOT_EXIST: matched = results_by_type.get(prereq.resource_type.value, []) for condition in prereq.conditions: if matched: findings.append(Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.FAIL, severity=prereq.severity, reason=f"Resource of type {prereq.resource_type.value} exists but must not", actual_value=len(matched) )) else: findings.append(Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.PASS, severity=prereq.severity, reason="No resources of this type found (correct)" )) continue # MUST_EXIST_BEFORE and OPTIONAL matched = results_by_type.get(prereq.resource_type.value, []) if not matched: for condition in prereq.conditions: status = FindingStatus.FAIL if prereq.intent == ResourceIntent.MUST_EXIST_BEFORE else FindingStatus.ERROR findings.append(Finding( prerequisite_id=prereq.id, condition=condition, status=status, severity=prereq.severity, reason=f"No {prereq.resource_type.value} resources found in subscription" )) continue for condition in prereq.conditions: finding = _evaluate_condition(condition, matched, prereq) findings.append(finding) return ValidationReport( scan_job_id=scan_job_id, cloud_provider=CloudProvider.AZURE, region=region, findings=findings, ) def _evaluate_condition(condition, scan_results: list, prereq) -&gt; Finding: """ Try condition against every matching ScanResult. PASS if any resource satisfies it. FAIL if no resource satisfies it. ERROR if the attribute could not be read from any resource. """ from app.core.types import ATTRIBUTE_TO_GROUP, Finding, FindingStatus from app.validation.evaluators import EVALUATOR_MAP group_name = ATTRIBUTE_TO_GROUP.get(condition.attribute) if not group_name: return Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.ERROR, severity=prereq.severity, reason=f"Attribute '{condition.attribute}' not in ATTRIBUTE_TO_GROUP" ) evaluator = EVALUATOR_MAP.get(condition.operator.value) if not evaluator: return Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.ERROR, severity=prereq.severity, reason=f"No evaluator for operator '{condition.operator.value}'" ) best_actual = None for result in scan_results: group = getattr(result, group_name, None) if group is None: continue actual = getattr(group, condition.attribute, None) best_actual = actual # track for error reporting if actual is None: continue try: passed = evaluator(actual, condition.expected_value) if passed: return Finding( prerequisite_id=prereq.id, resource_id=result.resource_id, resource_name=result.resource_name, condition=condition, status=FindingStatus.PASS, actual_value=actual, expected_value=condition.expected_value, severity=prereq.severity, reason=f"Condition met on {result.resource_name}" ) except Exception as e: log.warning("evaluator_error", attribute=condition.attribute, error=str(e)) # Nothing passed if best_actual is None: return Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.ERROR, severity=prereq.severity, reason=f"Attribute '{condition.attribute}' was None on all {len(scan_results)} resources" ) return Finding( prerequisite_id=prereq.id, condition=condition, status=FindingStatus.FAIL, actual_value=best_actual, expected_value=condition.expected_value, severity=prereq.severity, reason=f"No resource met condition: {condition.attribute} {condition.operator.value} {condition.expected_value}" ) ``` --- ## PHASE 7 — background tasks, API routes, main.py **Prompt for Claude:** ``` Build the API layer and background task system for infra-autopilot. PART A — background/runner.py async def update_job(db: AsyncSession, job_id: str, status: str, progress: int, step: str, error: str | None = None, result_id: str | None = None): """Update job row in DB. Called throughout background tasks.""" from app.models.job import Job from sqlalchemy import update await db.execute( update(Job).where(Job.id == job_id).values( status=status, progress_pct=progress, current_step=step, error_message=error, result_id=result_id ) ) await db.commit() PART B — background/extract_task.py async def run_extraction(document_id: str, job_id: str): """ Background task for extraction pipeline. Runs independently — creates its own DB session. Progress updates: 10% parse, 40% extract, 70% verify, 90% validate, 100% done. """ from app.database import AsyncSessionLocal from app.models.document import UploadedDocument, ParsedDocument from app.models.prerequisite import Prerequisite as PrerequisiteModel from app.models.review import ReviewQueueItem from app.extraction.graph import extraction_graph from app.background.runner import update_job async with AsyncSessionLocal() as db: try: await update_job(db, job_id, "running", 5, "loading_document") # Load document doc = await db.get(UploadedDocument, document_id) if not doc: await update_job(db, job_id, "failed", 0, "", error="Document not found") return await update_job(db, job_id, "running", 10, "parsing") # Run LangGraph initial_state = { "document_id": document_id, "file_bytes": doc.file_bytes, "file_type": doc.file_type, "raw_text": "", "sections": [], "tables": [], "parser_used": "", "parse_errors": [], "raw_requirements": [], "extract_attempts": 0, "requirements_with_flags": [], "prerequisites": [], "invalid_requirements": [], "errors": [], "completed": False, } # LangGraph invoke is sync in v0.2 — run in executor import asyncio loop = asyncio.get_event_loop() final_state = await loop.run_in_executor( None, lambda: extraction_graph.invoke(initial_state) ) await update_job(db, job_id, "running", 80, "saving_results") # Save ParsedDocument parsed = ParsedDocument( document_id=document_id, raw_text=final_state.get("raw_text", ""), sections=final_state.get("sections", []), tables=final_state.get("tables", []), parser_used=final_state.get("parser_used", "unknown"), parse_errors=final_state.get("parse_errors", []), ) db.add(parsed) # Save Prerequisites saved_count = 0 for prereq_dict in final_state.get("prerequisites", []): row = PrerequisiteModel( id=prereq_dict["id"], document_id=document_id, extraction_job_id=job_id, category=prereq_dict["category"], resource_type=prereq_dict["resource_type"], cloud_provider=prereq_dict["cloud_provider"], intent=prereq_dict["intent"], severity=prereq_dict["severity"], conditions=prereq_dict["conditions"], source_text=prereq_dict["source_text"], confidence=prereq_dict["confidence"], review_required=prereq_dict["review_required"], review_reason=prereq_dict.get("review_reason"), ) db.add(row) if prereq_dict["review_required"]: db.add(ReviewQueueItem( prerequisite_id=prereq_dict["id"], reason=prereq_dict.get("review_reason", "Flagged for review"), )) saved_count += 1 await db.commit() await update_job(db, job_id, "completed", 100, "done", result_id=document_id) log.info("extraction_complete", document_id=document_id, prerequisites=saved_count) except Exception as e: log.error("extraction_task_failed", document_id=document_id, error=str(e)) await update_job(db, job_id, "failed", 0, "", error=str(e)) PART C — background/scan_task.py async def run_scan(scan_job_id: str, document_id: str, resource_group: str | None, region: str): """ Background task for scan + validation pipeline. 1. Load approved prerequisites from DB 2. Run ScanOrchestrator 3. Save ScanResults 4. Run ValidationEngine 5. Save ValidationReport + Findings """ from app.database import AsyncSessionLocal from app.models.prerequisite import Prerequisite as PModel from app.models.scan_result import ScanResult as SRModel from app.models.report import ValidationReport as VRModel, Finding as FModel from app.scanner.orchestrator import ScanOrchestrator from app.validation.engine import ValidationEngine from app.config import get_settings from sqlalchemy import select from app.background.runner import update_job settings = get_settings() async with AsyncSessionLocal() as db: try: await update_job(db, scan_job_id, "running", 10, "loading_prerequisites") # Load approved prerequisites for this document result = await db.execute( select(PModel).where( PModel.document_id == document_id, PModel.review_required == False, ) ) prereq_rows = result.scalars().all() prereq_dicts = [ { "id": r.id, "category": r.category, "resource_type": r.resource_type, "cloud_provider": r.cloud_provider, "intent": r.intent, "severity": r.severity, "conditions": r.conditions, "source_text": r.source_text, "confidence": r.confidence, "review_required": r.review_required, } for r in prereq_rows ] if not prereq_dicts: await update_job(db, scan_job_id, "failed", 0, "", error="No approved prerequisites found") return await update_job(db, scan_job_id, "running", 20, "scanning_azure") orchestrator = ScanOrchestrator( subscription_id=settings.azure_subscription_id, tenant_id=settings.azure_tenant_id, client_id=settings.azure_client_id, client_secret=settings.azure_client_secret, ) import asyncio loop = asyncio.get_event_loop() scan_results = await loop.run_in_executor( None, lambda: orchestrator.run(prereq_dicts, resource_group=resource_group, region=region) ) await update_job(db, scan_job_id, "running", 70, "saving_scan_results") # Save scan results scan_result_dicts = [] for sr in scan_results: sr_dict = sr.model_dump(mode="json") # Determine which attribute group is populated and save it group_name = None attrs_dict = {} for group in ["compute","kubernetes","storage","database","virtual_network", "subnet","nsg","rbac","service_principal","enabled_service", "encryption","certificate","firewall_rule","compute_quota"]: val = sr_dict.get(group) if val is not None: group_name = group attrs_dict = val break row = SRModel( scan_job_id=scan_job_id, resource_id=sr.resource_id, resource_name=sr.resource_name, resource_type=sr.resource_type.value, cloud_provider=sr.cloud_provider.value, region=sr.region, tags=sr.tags, attributes=attrs_dict, attribute_group=group_name or "unknown", raw_response=sr_dict.get("raw_response", {}), ) db.add(row) scan_result_dicts.append(sr_dict) await db.commit() await update_job(db, scan_job_id, "running", 85, "validating") # Run validation engine = ValidationEngine() report = engine.run( prerequisites=prereq_dicts, scan_results=scan_result_dicts, scan_job_id=scan_job_id, region=region, ) # Save ValidationReport vr_row = VRModel( id=report.report_id, scan_job_id=scan_job_id, cloud_provider=report.cloud_provider.value, region=report.region, total=report.total, passed=report.passed, failed=report.failed, errors=report.errors, skipped=report.skipped, critical_failures=report.critical_failures, high_failures=report.high_failures, deployment_ready=report.deployment_ready, ) db.add(vr_row) # Save Findings for finding in report.findings: f_row = FModel( report_id=report.report_id, prerequisite_id=finding.prerequisite_id, resource_id=finding.resource_id, resource_name=finding.resource_name, condition=finding.condition.model_dump(mode="json"), status=finding.status.value, severity=finding.severity.value, actual_value=finding.actual_value, expected_value=finding.expected_value, reason=finding.reason, ) db.add(f_row) await db.commit() await update_job(db, scan_job_id, "completed", 100, "done", result_id=report.report_id) log.info("scan_complete", deployment_ready=report.deployment_ready, passed=report.passed, failed=report.failed) except Exception as e: log.error("scan_task_failed", error=str(e)) await update_job(db, scan_job_id, "failed", 0, "", error=str(e)) PART D — API routes api/documents.py: POST /documents Accept: multipart/form-data with file field Validate file extension is pdf or docx. Max size 50MB. Read file bytes. Compute SHA256 hash. Check if content_hash already exists in DB — if so return existing document_id with 200. Create UploadedDocument row. Create Job row (job_type="extraction", status="pending"). Dispatch background.tasks.extract_task.run_extraction(document_id, job_id) using FastAPI BackgroundTasks. Return 202: {job_id, document_id, message: "extraction started"} GET /documents/{document_id}/prerequisites Return all prerequisites for document with their review_required status. Group by review_required: {approved: [...], pending_review: [...]} api/jobs.py: GET /jobs/{job_id} Return: {id, job_type, status, progress_pct, current_step, error_message, result_id} Return 404 if not found. api/scans.py: POST /scans Body: {document_id: str, resource_group: str | None, region: str = "eastus"} Load prerequisites for document where review_required=False. If no approved prerequisites: return 400 with message. Create Job row (job_type="scan"). Dispatch background.tasks.scan_task.run_scan(job_id, document_id, resource_group, region) Return 202: {job_id, document_id} api/reports.py: GET /reports/{report_id} Load ValidationReport + all Findings. Return full report with findings grouped by status. Include summary: {deployment_ready, total, passed, failed, errors, skipped, critical_failures, high_failures} Findings sorted by severity (critical first), then status (fail first). api/review.py: GET /review Query param: document_id (optional filter) Return unresolved ReviewQueueItems with their prerequisite details. POST /review/{item_id}/resolve Body: {resolution: "approved" | "rejected" | "modified", note: str | None, modified_conditions: list | None} If resolution == "approved": Set review_required=False on prerequisite. Set resolved=True, resolution="approved" on review item. If resolution == "rejected": Set resolved=True. Prerequisite stays review_required=True (won't be scanned). If resolution == "modified": If modified_conditions provided: update prerequisite conditions. Set review_required=False, resolved=True. Return updated prerequisite. PART E — main.py from contextlib import asynccontextmanager from fastapi import FastAPI from fastapi.middleware.cors import CORSMiddleware import structlog from app.database import create_tables from app.api import documents, jobs, scans, reports, review log = structlog.get_logger(__name__) @asynccontextmanager async def lifespan(app: FastAPI): log.info("startup") await create_tables() yield log.info("shutdown") def create_app() -&gt; FastAPI: app = FastAPI( title="Infra Autopilot", description="Infrastructure prerequisite validation system", version="1.0.0", lifespan=lifespan, ) app.add_middleware( CORSMiddleware, allow_origins=["http://localhost:5173", "http://localhost:3000"], allow_methods=["*"], allow_headers=["*"], ) app.include_router(documents.router, prefix="/api/v1", tags=["documents"]) app.include_router(jobs.router, prefix="/api/v1", tags=["jobs"]) app.include_router(scans.router, prefix="/api/v1", tags=["scans"]) app.include_router(reports.router, prefix="/api/v1", tags=["reports"]) app.include_router(review.router, prefix="/api/v1", tags=["review"]) return app app = create_app() VERIFY all phases: uvicorn app.main:app --reload curl http://localhost:8000/docs # must show Swagger UI curl http://localhost:8000/api/v1/review # must return [] ``` --- ## PHASE 8 — React frontend **Prompt for Claude:** ``` Build the React frontend for infra-autopilot. Tech: React 18 + TypeScript + Vite + Tailwind CSS + React Query + React Router. SETUP: npm create vite@latest frontend -- --template react-ts cd frontend npm install @tanstack/react-query axios react-router-dom npm install -D tailwindcss postcss autoprefixer npx tailwindcss init -p frontend/src/types/index.ts — TypeScript interfaces: interface Job { id: string; job_type: "extraction" | "scan"; status: "pending" | "running" | "completed" | "failed"; progress_pct: number; current_step: string | null; error_message: string | null; result_id: string | null; } interface Prerequisite { id: string; resource_type: string; category: string; severity: string; intent: string; conditions: Array&lt;{attribute: string; operator: string; expected_value: any; unit: string | null}&gt;; source_text: string; confidence: number; review_required: boolean; review_reason: string | null; } interface Finding { prerequisite_id: string; resource_id: string | null; resource_name: string | null; condition: {attribute: string; operator: string; expected_value: any}; status: "pass" | "fail" | "error" | "skipped"; actual_value: any; expected_value: any; severity: string; reason: string | null; } interface ValidationReport { report_id: string; deployment_ready: boolean; total: number; passed: number; failed: number; errors: number; skipped: number; critical_failures: number; high_failures: number; findings: Finding[]; } interface ReviewItem { id: string; prerequisite_id: string; reason: string; resolved: boolean; prerequisite: Prerequisite; } frontend/src/api/client.ts: axios instance with baseURL: http://localhost:8000/api/v1 Content-Type: application/json frontend/src/hooks/useJobPoller.ts: useJobPoller(jobId: string | null) hook Uses useQuery with refetchInterval: (data) =&gt; (data?.status === "completed" || data?.status === "failed") ? false : 2000 Returns {job, isLoading, isError} frontend/src/pages/UploadPage.tsx: State: selectedFile (File | null), jobId (string | null), documentId (string | null) Upload form: Drag-and-drop zone that accepts PDF and DOCX only Shows filename when selected Upload button — POST to /documents with FormData On success: set jobId and documentId, show progress section Progress section (shows when jobId set): Uses useJobPoller(jobId) Progress bar showing progress_pct Current step text When status=completed: show "Extraction complete!" with link to review page When status=failed: show error_message in red frontend/src/pages/ReviewPage.tsx: URL: /review?document_id=X Fetch GET /review?document_id=X Shows list of prerequisites needing review: For each ReviewItem: Shows resource_type, severity badge, confidence percentage Shows source_text in a quote block Shows conditions list Shows review_reason Two buttons: Approve / Reject Approve → POST /review/{id}/resolve {resolution: "approved"} Reject → POST /review/{id}/resolve {resolution: "rejected"} "Start Scan" button (disabled if any items still pending): Opens modal asking for: resource_group (optional), region (default eastus) On confirm: POST /scans {document_id, resource_group, region} On success: navigate to /scan-progress/{job_id} frontend/src/pages/ScanProgressPage.tsx: URL: /scan-progress/:jobId Uses useJobPoller(jobId) Shows animated progress bar Shows current_step When completed: auto-navigate to /reports/{result_id} When failed: show error with retry button frontend/src/pages/ReportPage.tsx: URL: /reports/:reportId Fetch GET /reports/{reportId} Top section: deployment badge Green "READY TO DEPLOY" if deployment_ready=true Red "NOT READY" if false Summary counts: total / passed / failed / errors / skipped Critical failures count in red if &gt; 0 Findings table: Columns: Severity | Resource Type | Attribute | Operator | Expected | Actual | Status | Reason Row colors: red for fail/critical, orange for fail/high, yellow for error, green for pass, gray for skipped Sorted: critical fails first, then high fails, then others Filterable by status (All / Pass / Fail / Error / Skipped) frontend/src/App.tsx: BrowserRouter with routes: / → UploadPage /review → ReviewPage /scan-progress/:jobId → ScanProgressPage /reports/:reportId → ReportPage QueryClientProvider wrapping everything Simple navbar with app name and navigation links VERIFY: npm run dev — must start on port 5173 Navigate to http://localhost:5173 — must show upload page ``` --- ## PHASE 9 — Integration, and run **Prompt for Claude:** ``` Final integration and verification for infra-autopilot. STEP 1 — Database setup: createdb infraautopilot (or use psql to create it) alembic upgrade head Verify: alembic current shows head revision STEP 2 — Start the backend: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 Visit http://localhost:8000/docs Verify all endpoints are visible with correct request/response schemas. STEP 3 — Start the frontend: cd frontend &amp;&amp; npm run dev Visit http://localhost:5173 Verify upload page loads without console errors. Test ValidationEngine.run() with: Fixture: one KUBERNETES_CLUSTER prerequisite with k8s_version GTE 1.28, CRITICAL Fixture: one ScanResult with kubernetes.k8s_version = "1.29.2" Expected: deployment_ready=True, passed=1, failed=0 Fixture: same prerequisite Fixture: ScanResult with kubernetes.k8s_version = "1.25.0" Expected: deployment_ready=False, failed=1, critical_failures=1 Fixture: prerequisite with WILL_BE_CREATED intent No scan results needed Expected: skipped=1, deployment_ready=True STEP 6 — Verify imports don't have circular dependencies: python -c "from app.core.types import *; print('types ok')" python -c "from app.extraction.graph import extraction_graph; print('graph ok')" python -c "from app.scanner.orchestrator import ScanOrchestrator; print('orchestrator ok')" python -c "from app.validation.engine import ValidationEngine; print('validation ok')" STEP 7 — Full flow test (manual): 1. Upload a test PDF via POST /docs Upload endpoint in Swagger UI 2. Poll GET /jobs/{job_id} until completed 3. Check GET /documents/{document_id}/prerequisites — should show extracted prerequisites 4. If any in review: POST /review/{id}/resolve with approved 5. POST /scans with the document_id 6. Poll GET /jobs/{scan_job_id} until completed 7. GET /reports/{result_id} — should show ValidationReport with findings